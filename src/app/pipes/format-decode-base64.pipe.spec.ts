import { FormatDecodeBase64Pipe } from './format-decode-base64.pipe';

describe('FormatDecodeBase64Pipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDecodeBase64Pipe();
    expect(pipe).toBeTruthy();
  });
});
